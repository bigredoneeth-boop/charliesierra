// Re-export AuditEventType from generated backend bindings as the canonical source.
// This file is kept as a convenience re-export so existing imports remain stable.
export { AuditEventType } from "@/backend";
